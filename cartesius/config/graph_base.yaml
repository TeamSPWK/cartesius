# Metadata
parent_config: "transformer.yaml"
description: "Best configuration to train a basic graph-based model."

# Model
model_name: "graph"
d_model: 64
d_ff: 512
dropout: 0.1
n_layers: 2

# Training
lr: 1e-3

# Data handling
tokenizer: "graph"
